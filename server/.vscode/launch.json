{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "debug with make",
      "type": "cppdbg",
      "request": "launch",
      "cwd": "${workspaceFolder}",
      "program": "${workspaceFolder}/build/main",
      "MIMode": "gdb",
      "preLaunchTask": "debug make",
      "miDebuggerPath": "gdb"
    }
  ]
}